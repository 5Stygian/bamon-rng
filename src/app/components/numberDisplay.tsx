"use client";

import { config } from "../config";

export default function NumberDisplay() {
  return (
    <div id="NumberDisplay" className={config.styles.NumberDisplay}>
      ??????
    </div>
  );
}
