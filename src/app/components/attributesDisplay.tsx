"use client";

import { config } from "../config";

export default function AttributeDisplay() {
  return (
    <div id="NumberDisplayOuter" className={config.styles.AttributeDisplay}>
      <div id="AttributeDisplay" />
    </div>
  );
}
